// Workspaces are admin-created (domain-derived membership makes self-serve
// creation unsafe), so every surface that mentions teams points here instead
// of at a flow that doesn't exist.
export const TEAMS_CONTACT_EMAIL = "sam@joinstash.ai";
