"""Native-shape PPTX/Slides export sandbox.

Experimental — NOT wired into the celery exporter registry or the
/api/v1/pages/{id}/export route. Driven only by `python -m
backend.exports.native.cli` and the tests in this package until the
output quality justifies promotion.

See `/Users/samzliu/.claude/plans/look-into-the-options-jazzy-pike.md`
for the design.
"""
