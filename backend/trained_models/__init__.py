"""Trained models: a paid training run produces a model the user then uses
without limit. Skills bring the tools; this package brings the machinery.

Everything a kind of model shares lives here — purchases, model rows, the
training job, the MCP mount, the page endpoints. Everything specific to one
recipe lives in `kinds/<name>/`: how to judge a corpus, how to start and read
a GPU job, and which tools an agent gets.
"""
