{
  "_comment": "Merged into ~/.cursor/hooks.json by `stash signin` (or copy by hand for <project>/.cursor/hooks.json). Commands are machine-independent — `stash hook run cursor <event>` executes the scripts shipped inside the stashai package. Cursor timeouts are in SECONDS.",
  "version": 1,
  "hooks": {
    "sessionStart": [
      { "command": "stash hook run cursor on_session_start", "timeout": 5 }
    ],
    "beforeSubmitPrompt": [
      { "command": "stash hook run cursor on_prompt", "timeout": 3 }
    ],
    "postToolUse": [
      { "command": "stash hook run cursor on_tool_use", "timeout": 5 }
    ],
    "afterAgentResponse": [
      { "command": "stash hook run cursor on_agent_response", "timeout": 10 }
    ],
    "sessionEnd": [
      { "command": "stash hook run cursor on_session_end", "timeout": 5 }
    ]
  }
}
