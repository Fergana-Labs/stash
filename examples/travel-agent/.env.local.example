# Copy to .env.local and fill in. Next loads .env.local automatically, so
# `npm run dev` is then enough.
STASH_API_KEY=
ANTHROPIC_API_KEY=
# Omit for a local backend on :3456
# STASH_BASE_URL=http://localhost:3456
